import { SettingsSection } from "@/components/settings/settings-section"

export default function SecuritySettingsPage() {
  return (
    <SettingsSection
      title="Security"
      description="Sessions, password, privacy controls, and device management."
    />
  )
}

